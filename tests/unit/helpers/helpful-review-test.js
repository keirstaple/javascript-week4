import { helpfulReview } from 'js-week4/helpers/helpful-review';
import { module, test } from 'qunit';

module('Unit | Helper | helpful review');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = helpfulReview([42]);
  assert.ok(result);
});
